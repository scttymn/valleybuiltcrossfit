Early exploration of the PushPress API: a week of classes pulled from the
Platform API and a standalone HTML view of it. Superseded by the live schedule in
the app, kept only as a reference for the shape of PushPress's data.
